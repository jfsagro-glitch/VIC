# Настройка GitHub Actions для автоматического деплоя

## ✅ Что уже сделано:

1. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
2. ✅ Код загружен в репозиторий
3. ✅ Настроен CI workflow для тестирования

## 🔧 Следующие шаги:

### 1. Настройте GitHub Secrets

Перейдите в репозиторий: https://github.com/jfsagro-glitch/VIC/settings/secrets/actions

Добавьте следующие секреты:

#### Для деплоя на Vercel (рекомендуется):

1. **VERCEL_TOKEN**
   - Получите на: https://vercel.com/account/tokens
   - Создайте новый токен и скопируйте

2. **VERCEL_ORG_ID** и **VERCEL_PROJECT_ID**
   - Вариант 1: Создайте проект на Vercel вручную один раз, затем найдите ID в настройках
   - Вариант 2: Используйте Vercel CLI:
     ```bash
     npm install -g vercel
     cd client
     vercel link
     ```
     Это создаст файл `.vercel/project.json` с нужными ID

3. **REACT_APP_API_URL** (опционально)
   - URL вашего backend API
   - По умолчанию: `http://localhost:5000/api`

### 2. Проверьте workflow

1. Перейдите в **Actions** вкладку репозитория
2. После следующего push в `main` ветку workflow запустится автоматически
3. Или запустите вручную: **Actions** → **Deploy Frontend** → **Run workflow**

### 3. Альтернативные варианты деплоя

#### Вариант A: Netlify

Замените шаг деплоя в `.github/workflows/deploy.yml`:

```yaml
- name: Deploy to Netlify
  uses: nwtgck/actions-netlify@v2.0
  with:
    publish-dir: './client/build'
    production-branch: main
    github-token: ${{ secrets.GITHUB_TOKEN }}
    deploy-message: "Deploy from GitHub Actions"
  env:
    NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
    NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
```

#### Вариант B: GitHub Pages

```yaml
- name: Deploy to GitHub Pages
  uses: peaceiris/actions-gh-pages@v3
  with:
    github_token: ${{ secrets.GITHUB_TOKEN }}
    publish_dir: ./client/build
```

### 4. Проверка работы

После успешного деплоя:
- ✅ Проверьте что сайт доступен
- ✅ Проверьте что все стили загружаются
- ✅ Проверьте что API запросы работают (если backend настроен)

## 📝 Структура workflow

Текущий workflow делает следующее:

1. **Checkout code** - получает код из репозитория
2. **Setup Node.js** - устанавливает Node.js 18
3. **Install dependencies** - устанавливает зависимости клиента
4. **Build** - собирает React приложение
5. **Deploy to Vercel** - деплоит на Vercel (только для main ветки)
6. **Upload artifacts** - сохраняет build артефакты

## 🚨 Troubleshooting

### Workflow не запускается:
- Проверьте что файл находится в `.github/workflows/`
- Убедитесь что ветка называется `main` или `master`
- Проверьте синтаксис YAML файла

### Ошибка при деплое:
- Проверьте что все секреты настроены
- Проверьте логи в Actions вкладке
- Убедитесь что Vercel проект создан

### Build не проходит:
- Проверьте что все зависимости указаны в `package.json`
- Проверьте что `REACT_APP_API_URL` настроен правильно
- Посмотрите логи сборки

## 📚 Дополнительные ресурсы

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Vercel документация](https://vercel.com/docs)
- [Netlify документация](https://docs.netlify.com)

