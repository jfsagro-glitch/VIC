# Быстрый деплой Frontend

## 🚀 Деплой на Vercel (Самый простой способ)

### Через веб-интерфейс (Рекомендуется):

1. **Перейдите на [vercel.com](https://vercel.com)** и войдите через GitHub

2. **Нажмите "Add New Project"**

3. **Импортируйте репозиторий** `jfsagro-glitch/VIC`

4. **Настройте проект:**
   - **Framework Preset**: Create React App
   - **Root Directory**: `client`
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
   - **Install Command**: `npm install`

5. **Добавьте переменные окружения:**
   - `REACT_APP_API_URL` = `http://localhost:5000/api` (для начала, потом замените на URL вашего backend)

6. **Нажмите "Deploy"**

7. **Готово!** Vercel автоматически задеплоит ваш проект и даст вам URL

### Через CLI:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите
vercel login

# Перейдите в папку client
cd client

# Деплой
vercel

# Следуйте инструкциям
```

## 🌐 Деплой на Netlify

1. **Перейдите на [netlify.com](https://netlify.com)** и войдите через GitHub

2. **Нажмите "Add new site" → "Import an existing project"**

3. **Выберите репозиторий** `jfsagro-glitch/VIC`

4. **Настройки:**
   - **Base directory**: `client`
   - **Build command**: `npm run build`
   - **Publish directory**: `client/build`

5. **Добавьте переменные окружения:**
   - `REACT_APP_API_URL` = `http://localhost:5000/api`

6. **Нажмите "Deploy site"**

## 📝 После деплоя

1. **Обновите `REACT_APP_API_URL`** в настройках платформы на URL вашего backend API

2. **Проверьте работу:**
   - Откройте задеплоенный сайт
   - Проверьте консоль браузера на ошибки
   - Попробуйте создать проект

## 🔧 Локальный запуск для тестирования

```bash
# Установите зависимости
cd client
npm install

# Запустите dev сервер
npm start
```

Откроется на `http://localhost:3000`

## ⚠️ Важно

- Убедитесь, что backend доступен по указанному URL
- Проверьте CORS настройки на backend
- Для продакшена используйте HTTPS

