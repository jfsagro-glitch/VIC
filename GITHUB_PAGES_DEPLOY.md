# Деплой на GitHub Pages

## ✅ Преимущества GitHub Pages

- ✅ **Бесплатно** - встроено в GitHub
- ✅ **Не требует дополнительных сервисов** - все на GitHub
- ✅ **Автоматический деплой** - при каждом push
- ✅ **HTTPS по умолчанию** - безопасное соединение
- ✅ **Простая настройка** - один раз настроил и работает

## 🚀 Быстрый старт

### Шаг 1: Включите GitHub Pages в настройках репозитория

1. Перейдите в **Settings** вашего репозитория
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
4. Сохраните изменения

### Шаг 2: Настройте переменные окружения (опционально)

Перейдите в **Settings** → **Secrets and variables** → **Actions**

Добавьте (если нужно):
- `REACT_APP_API_URL` - URL вашего backend API

### Шаг 3: Запустите workflow

1. Перейдите во вкладку **Actions**
2. Выберите workflow **Deploy to GitHub Pages**
3. Нажмите **Run workflow**
4. Или просто сделайте push в `main` ветку

## 📍 URL вашего сайта

После успешного деплоя ваш сайт будет доступен по адресу:

```
https://jfsagro-glitch.github.io/VIC/
```

## 🔧 Настройка кастомного домена (опционально)

1. В настройках **Pages** добавьте ваш домен
2. Добавьте секрет `CUSTOM_DOMAIN` в GitHub Secrets
3. Workflow автоматически настроит CNAME

## 📝 Что делает workflow

1. **Checkout** - получает код из репозитория
2. **Setup Node.js** - устанавливает Node.js 18
3. **Install dependencies** - устанавливает зависимости
4. **Build** - собирает React приложение
5. **Deploy** - деплоит на GitHub Pages

## ⚠️ Важные замечания

### React Router

Для правильной работы React Router добавлен файл `client/public/_redirects` который перенаправляет все запросы на `index.html`.

### API URL

Убедитесь, что `REACT_APP_API_URL` указывает на публично доступный backend. GitHub Pages работает только со статическими файлами, поэтому backend должен быть на отдельном сервере.

### Ограничения GitHub Pages

- Только статические файлы (HTML, CSS, JS)
- Максимальный размер репозитория: 1GB
- Максимальный размер файла: 100MB
- Пропускная способность: 100GB/месяц

## 🔄 Обновление сайта

Сайт автоматически обновляется при каждом push в `main` ветку. Просто:

```bash
git add .
git commit -m "Update"
git push
```

И через несколько минут изменения появятся на сайте!

## 🐛 Troubleshooting

### Сайт не обновляется
- Проверьте что workflow выполнился успешно в Actions
- Убедитесь что Pages включены в Settings
- Проверьте что используется правильная ветка (main/master)

### 404 ошибки на роутах
- Убедитесь что файл `client/public/_redirects` существует
- Проверьте что в `package.json` указан правильный `homepage`

### API запросы не работают
- Проверьте CORS настройки на backend
- Убедитесь что `REACT_APP_API_URL` настроен правильно
- Проверьте что backend доступен публично

## 📚 Дополнительная информация

- [GitHub Pages документация](https://docs.github.com/en/pages)
- [GitHub Actions документация](https://docs.github.com/en/actions)

